---
title: "Uni-LaDiR: Latent Diffusion Unifies Multimodal Reasoning"
title_zh: Uni-LaDiR：潜在扩散统一多模态推理
authors: "Haoqiang Kang, Yizhe Zhang, Nikki Lijing Kuang, Yian Ma, Lianhui Qin"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.19878v1"
tags: ["query:demo"]
score: 8.0
evidence: 在共享潜在思维空间统一多模态推理的扩散框架
tldr: "多模态推理需跨模态调用信息，但现有方法常把各模态思维 token 拼接在单一序列，模型需自行弥合表示差异。Uni-LaDiR 将不同模态的教师推理步骤映射到共享潜空间，用统一编码器生成共享 thought tokens，并以扩散模型从输入和前序块预测下一块思维 token。编码器与扩散推理器共享权重联合训练，使 token 既任务有用又可预测。在 11 个 VLM 基准和 2 个 VLA 套件上，视觉推理与机器人操作分别相对最强基线提升 7.3% 和 6.1%，表明共享潜空间扩散推理能统一跨模态推理并提升下游任务。"
source: arxiv
selection_source: carryover_cache
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.19878v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 2172, \"height\": 724}, {\"url\": \"assets/figures/arxiv/2609.19878v1/fig-002.webp\", \"caption\": \"\", \"page\": 2, \"index\": 2, \"width\": 2172, \"height\": 724}]"
motivation: 现有多模态推理多将各模态思维 token 拼成单序列，模型需自行对齐表示，跨模态推理存在鸿沟。
method: Uni-LaDiR 用统一编码器把教师推理步骤映射为共享 thought tokens，并用扩散模型从上下文预测下一块思维 token，与编码器共享权重联合训练。
result: "在 11 个 VLM 基准和 2 个 VLA 套件上，视觉推理相对最强基线提升 7.3%，机器人操作提升 6.1%。"
conclusion: 共享潜空间扩散推理可统一多模态思维，避免显式拼接，为跨模态推理与动作生成提供新范式。
---

## 摘要
多模态推理要求模型在整个推理过程中利用来自多种模态的信息。然而，现有方法通常将特定模态的思维 token 拼接在单一序列中，使模型在跨模态推理时自行弥合表示差异。我们提出 Uni-LaDiR（统一潜在扩散推理器），这是一个将这些思维带入共享潜在空间进行推理的框架。一个统一编码器将来自不同模态的教师推理步骤映射为共享思维 token，并通过训练保留后续推理步骤以及最终答案或动作所需的信息。由于同一上下文可以支持多个有效的下一步，我们使用扩散模型根据输入和先前块预测下一块思维 token。以共享模型权重联合训练编码器和扩散推理器，促使思维 token 既对任务有用，又可由可用上下文预测。在推理时，模型无需教师观测即可生成这些 token。在十一个视觉语言模型（VLM）基准和两个视觉-语言-动作（VLA）套件上，Uni-LaDiR 相较于评估的最强基线，在视觉推理任务上取得 7.3% 的相对提升，在机器人操作任务上取得 6.1% 的相对提升。

## Abstract
Multimodal reasoning requires models to draw on information from multiple modalities throughout the reasoning process. Yet existing methods often concatenate modality-specific thought tokens in a single sequence, leaving the model to bridge representational differences as it reasons across modalities. We introduce Uni-LaDiR (Unified Latent Diffusion Reasoner), a framework that brings these thoughts into a shared latent space for reasoning. A unified encoder maps teacher reasoning steps from different modalities into shared thought tokens, trained to preserve the information needed for later reasoning steps and the final answer or action. Because the same context can support multiple valid next steps, we use diffusion to predict the next block of thought tokens from the input and preceding blocks. Jointly training the encoder and diffusion reasoner with shared model weights encourages thought tokens to be both useful for the task and predictable from the available context. At inference, the model generates these tokens without teacher observations. Across eleven vision-language model (VLM) benchmarks and two vision-language-action (VLA) suites, Uni-LaDiR achieves relative gains over the strongest evaluated baselines of 7.3% on visual reasoning tasks and 6.1% on robot manipulation tasks.

---

## 论文详细总结（自动生成）

# Uni-LaDiR：潜在扩散统一多模态推理——中文总结

## 1. 核心问题与整体含义

- **研究背景**：多模态推理要求模型在推理全过程中综合利用文本、图像、3D 点云、机器人状态等模态信息。现有方法通常将各模态编码为各自 token/特征，再拼接或交错到同一 LLM 序列中。
- **核心问题**：这种“模态特定表示 + 序列拼接”的方式，使推理动态与跨模态表示转换纠缠在一起。模型不仅要决定推理状态如何演化，还要决定该状态如何转换为下一模态的表示形式，形成“模态切换鸿沟”。
- **论文主张**：感知可以保持模态依赖，但推理动态应运行在统一的共享潜在推理空间中。每个模态负责抽取证据，而推理只需要任务相关、跨模态共享的信息。
- **整体含义**：Uni-LaDiR 将不同模态教师推理步骤映射为共享 thought tokens，并用扩散模型在潜空间中预测下一块思维 token，从而避免显式拼接模态特定 latent，统一 VLM 与 VLA 的中间推理接口。

## 2. 方法论

### 2.1 核心思想

- 用**统一编码器**把来自文本、图像、3D、机器人状态等不同模态的教师推理步骤，映射到同一个共享潜在推理空间。
- 每个教师步骤被编码为一个固定宽度的 latent block，即若干 **thought tokens**。
- thought tokens 不要求重建源模态，而要求满足两个性质：
  - **推理充分性**：保留后续推理步骤和最终答案/动作所需信息。
  - **上下文可预测性**：在推理时可由输入和前序 thought tokens 预测出来。
- 用**扩散/流匹配**建模下一块 thought tokens 的条件分布，以处理同一上下文对应多个有效下一步的情况。

### 2.2 构造共享 thought tokens

- 对第 \(i\) 个教师推理步骤，先用模态特定编码器得到特征 \(s_i\)。
- 在 \(s_i\) 后附加一组固定长度的可学习 embedding \(q\)。
- 统一编码器将 \(q\) 位置最后隐藏状态映射为干净 thought tokens \(z_i^\star\)。
- 所有模态共享同一套 \(q\)、输出投影和每块 token 数量。
- 训练时可将多个教师步骤打包进一次前向，并用构造注意力掩码隔离各步骤：每个 \(q\) 只关注自身及关联教师特征，防止跨推理步骤信息泄漏。

### 2.3 下游 grounding 监督

- 给定输入 \(x\) 和前序 thought 前缀 \(z_{<i}^\star\)，共享骨干预测下一教师目标
